# MongoDB (required)
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/task-tracker?retryWrites=true&w=majority

# JWT secret for auth tokens (optional; set a long random string in production)
JWT_SECRET=your-secret-key-change-in-production

# Frontend URL for CORS (optional; set in production e.g. https://your-app.vercel.app)
# FRONTEND_URL=

# First admin: user with this email gets role "admin" on register or next login (optional)
# FIRST_ADMIN_EMAIL=admin@example.com
