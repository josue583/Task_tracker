# Backend API URL (used at build time).
# Local dev: leave empty (defaults to http://localhost:5000).
# Production: set to your backend URL, e.g. https://task-tracker-1-ioxm.onrender.com
VITE_API_URL=
